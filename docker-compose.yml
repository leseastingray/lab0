version: '3'
services:
    mono:
        build:
            context: ./
            dockerfile: ./mono.dockerfile
        environment:
            DISPLAY: $DISPLAY
        volumes:    
            - "/tmp/.X11-unix:/tmp/.X11-unix:rw"
            - '.:/root/lab0'
